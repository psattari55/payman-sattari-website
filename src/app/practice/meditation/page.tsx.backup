// src/app/practice/meditation/page.tsx
'use client'

import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { Eye, Anchor, Wind, Orbit } from 'lucide-react';
import Section from '@/components/ui/Section';
import PageTransition from '@/components/ui/PageTransition';

const meditationTypes = [
  {
    title: "Observer Meditation",
    description: "Develop the practice of neutral awareness and non-reactivity",
    icon: Eye,
    steps: [
      "Find a quiet, comfortable space",
      "Watch thoughts without engaging",
      "Practice non-judgment",
      "Maintain inner stillness",
      "Notice but don't react"
    ],
    benefits: [
      "Clearer perception",
      "Reduced reactivity",
      "Better decision-making",
      "Increased self-awareness"
    ]
  },
  {
    title: "Grounding Meditation",
    description: "Connect with Earth's energy and establish stability",
    icon: Anchor,
    steps: [
      "Stand barefoot if possible",
      "Focus below your feet",
      "Connect with Earth's energy",
      "Draw energy upward",
      "Cycle through your body"
    ],
    benefits: [
      "Enhanced stability",
      "Increased presence",
      "Physical centering",
      "Energetic balance"
    ]
  }
];

export default function MeditationPage() {
  const [activeTab, setActiveTab] = useState('understanding');
  const [selectedPractice, setSelectedPractice] = useState<number | null>(null);

  return (
    <PageTransition>
      <div className="min-h-screen bg-gradient-to-b from-gray-50 to-white">
        <Section width="default" className="pt-24 pb-16">
          <div className="max-w-4xl mx-auto">
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6 }}
              className="text-center mb-12"
            >
              <h1 className="text-4xl font-normal mb-6 tracking-wide text-gray-900">
                The Art of Meditation
              </h1>
              <p className="text-xl text-gray-600 max-w-3xl mx-auto">
                More than a mental exercise, meditation is a journey of conscious self-discovery and inner exploration.
              </p>
            </motion.div>

            {/* Navigation Tabs */}
            <div className="flex justify-center mb-12">
              <div className="flex space-x-2 p-1 bg-gray-100 rounded-lg">
                {['understanding', 'practice', 'techniques'].map((tab) => (
                  <motion.button
                    key={tab}
                    onClick={() => setActiveTab(tab)}
                    className={`px-4 py-2 rounded-md text-sm font-medium transition-colors ${
                      activeTab === tab 
                        ? 'bg-white text-gray-900 shadow-sm' 
                        : 'text-gray-600 hover:text-gray-900'
                    }`}
                    whileHover={{ scale: 1.02 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    {tab.charAt(0).toUpperCase() + tab.slice(1)}
                  </motion.button>
                ))}
              </div>
            </div>

            {/* Content Sections */}
            <motion.div
              key={activeTab}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.4 }}
              className="prose prose-lg max-w-none"
            >
              {activeTab === 'understanding' && (
                <div className="space-y-6">
                  <p className="text-lg text-gray-700">
                    Meditation is the practice of looking inward—a tool for consciously exploring and dealing with all the 
                    elements of our inner life. Through meditation, we develop greater self-awareness and the ability to 
                    observe and interact with our inner world with clarity.
                  </p>
                  
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8">
                    <div className="bg-white border border-gray-200 rounded-lg p-6">
                      <div className="flex items-center gap-3 mb-4">
                        <Wind className="w-5 h-5 text-blue-600" />
                        <h3 className="font-medium text-gray-900">Beyond Thinking</h3>
                      </div>
                      <p className="text-gray-600 text-sm">
                        Meditation is more than thinking. In fact, many meditations are exactly about moving out of a "thinking" state and into a "being" state. It is about engaging with the full spectrum of our inner experience.
                      </p>
                    </div>
                    
                    <div className="bg-white border border-gray-200 rounded-lg p-6">
                      <div className="flex items-center gap-3 mb-4">
                        <Orbit className="w-5 h-5 text-blue-600" />
                        <h3 className="font-medium text-gray-900">Inner Exploration</h3>
                      </div>
                      <p className="text-gray-600 text-sm">
                        Through meditation, we explore thoughts, emotions, beliefs, and physical sensations with 
                        non-judgmental awareness, developing deeper self-understanding, and the capacity to work with these elements consciously.
                      </p>
                    </div>
                  </div>

                  <div className="bg-stone-50 border border-stone-100 rounded-lg p-6 my-8">
                    <h3 className="text-lg font-medium text-gray-900 mb-4">Key Tools & Practices</h3>
                    <ul className="space-y-4">
                      <li className="flex items-start gap-3">
                        <span className="font-medium text-stone-400">01.</span>
                        <div>
                          <p className="font-medium text-gray-900">Non-reactivity</p>
                          <p className="text-gray-600 text-sm mt-1">
                            Cultivating the ability to observe without immediate reaction or judgment
                          </p>
                        </div>
                      </li>
                      <li className="flex items-start gap-3">
                        <span className="font-medium text-stone-400">02.</span>
                        <div>
                          <p className="font-medium text-gray-900">Self-awareness</p>
                          <p className="text-gray-600 text-sm mt-1">
                            Developing deeper understanding of our inner landscape
                          </p>
                        </div>
                      </li>
                      <li className="flex items-start gap-3">
                        <span className="font-medium text-stone-400">03.</span>
                        <div>
                          <p className="font-medium text-gray-900">Inner Transformation</p>
                          <p className="text-gray-600 text-sm mt-1">
                            Using awareness to consciously shape our inner reality
                          </p>
                        </div>
                      </li>
                    </ul>
                  </div>
                </div>
              )}

              {activeTab === 'practice' && (
                <div className="space-y-6">
                  <p className="text-lg text-gray-700">
                    Meditation doesn't need to be complicated. It requires only space, time, and clear intention. 
                    As long as you're looking inward with curiosity and openness, you're meditating effectively.
                  </p>

                  <div className="bg-white border border-gray-200 rounded-lg p-6 my-8">
                    <h3 className="text-lg font-medium text-gray-900 mb-6">Essential Elements</h3>
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                      <div className="space-y-2">
                        <h4 className="font-medium text-gray-900">Space</h4>
                        <p className="text-sm text-gray-600">
                          Find a quiet, comfortable location where you won't be disturbed
                        </p>
                      </div>
                      <div className="space-y-2">
                        <h4 className="font-medium text-gray-900">Time</h4>
                        <p className="text-sm text-gray-600">
                          Set aside dedicated time for practice, even if just a few minutes
                        </p>
                      </div>
                      <div className="space-y-2">
                        <h4 className="font-medium text-gray-900">Intention</h4>
                        <p className="text-sm text-gray-600">
                          Approach practice with clear purpose and gentle determination
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {activeTab === 'techniques' && (
                <div className="space-y-6">
                  {meditationTypes.map((type, index) => {
                    const Icon = type.icon;
                    return (
                      <motion.div
                        key={index}
                        className={`bg-white border border-gray-200 rounded-lg overflow-hidden ${
                          selectedPractice === index ? 'ring-2 ring-blue-500' : ''
                        }`}
                        onClick={() => setSelectedPractice(selectedPractice === index ? null : index)}
                      >
                        <div className="p-6 cursor-pointer">
                          <div className="flex items-center gap-3 mb-3">
                            <Icon className="w-5 h-5 text-blue-600" />
                            <h3 className="font-medium text-gray-900">{type.title}</h3>
                          </div>
                          <p className="text-gray-600">{type.description}</p>
                        </div>
                        
                        {selectedPractice === index && (
                          <motion.div
                            initial={{ height: 0, opacity: 0 }}
                            animate={{ height: 'auto', opacity: 1 }}
                            exit={{ height: 0, opacity: 0 }}
                            transition={{ duration: 0.3 }}
                            className="border-t border-gray-100"
                          >
                            <div className="p-6 bg-gray-50">
                              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                                <div>
                                  <h4 className="font-medium text-gray-900 mb-3">Practice Steps</h4>
                                  <ul className="space-y-2">
                                    {type.steps.map((step, i) => (
                                      <li key={i} className="flex items-center gap-2 text-sm text-gray-600">
                                        <span className="w-5 h-5 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center text-xs">
                                          {i + 1}
                                        </span>
                                        {step}
                                      </li>
                                    ))}
                                  </ul>
                                </div>
                                <div>
                                  <h4 className="font-medium text-gray-900 mb-3">Benefits</h4>
                                  <ul className="space-y-2">
                                    {type.benefits.map((benefit, i) => (
                                      <li key={i} className="flex items-center gap-2 text-sm text-gray-600">
                                        <span className="text-blue-600">•</span>
                                        {benefit}
                                      </li>
                                    ))}
                                  </ul>
                                </div>
                              </div>
                            </div>
                          </motion.div>
                        )}
                      </motion.div>
                    );
                  })}
                </div>
              )}
            </motion.div>
          </div>
        </Section>
      </div>
    </PageTransition>
  );
}